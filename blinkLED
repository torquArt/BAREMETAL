#include <avr/io.h> 
#include <util/delay.h>

int main (){
  DDRB = (1 << PB5);
  while(1){
    PORTB = PORTB|(1 << PB5) | (1 << PB2); //00010000
    
    _delay_ms(1000);

    //PORTB = (0); //joga todos pra 0
    PORTB = PORTB & (~(1<<PB5));
    _delay_ms(1000);

    }
  }
